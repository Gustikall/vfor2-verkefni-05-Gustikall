<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
</head>
<body>
    <div class="LoginBox">

        <label for="email"><b>Email</b></label>
        <input type="email" placeholder="Enter Your Email Here" name="email" required>

        <label for="pw"><b>Password</b></label>
        <input type="password" placeholder="Enter Your Password Here" name="pw" required>

        <button type="submit">Login</button>
        <a href="/register" class="MiniRegister">Register</a>
    </div>
    <div class="FrontPage">
        <div class="leikir">
            <%
                for(let i =0; i < data.games.length;i++){
            %>
            <div>
                <div>
                    <h1><%=data.games[i].name %></h1>
                    <img src="/images/<%=data.games[i].profile %>" height="250" width="250"/>
                    <div><a href="<%=data.games[i].link%>"><%=data.games[i].link%></a></div>
                </div>
            </div>
            <%}%>
        </div>
    </div>

</body>
</html>